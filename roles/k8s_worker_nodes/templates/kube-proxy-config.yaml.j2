apiVersion: kubeproxy.config.k8s.io/v1alpha1
kind: KubeProxyConfiguration
clientConnection:
  kubeconfig: "/var/lib/kube-proxy/kubeconfig"
bindAddress: "{{ item.value.ip_address }}"
mode: "iptables"
clusterCIDR: "{{ k8s_cluster_cidr }}"